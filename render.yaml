services:
  - type: web
    name: gatto-estudio-demo
    runtime: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    branch: main
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
